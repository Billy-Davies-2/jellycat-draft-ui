// Dev-only hint: we no longer eagerly touch the DB here to avoid bundling pg/fs.
// Left as a noop placeholder so existing imports are harmless if still present.
;(function noop() {
  return
})()

export {}
